---
type Props = {
  heading?: string;
};

const { heading } = Astro.props;
---

<div class="flex flex-col items-start gap-4 p-8">
  {heading ? <h3 class="font-medium text-white">{heading}</h3> : null}
  <div class="flex flex-col gap-4 text-neutral-400">
    <slot />
  </div>
</div>
